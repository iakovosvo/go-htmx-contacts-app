package templates

import "github.com/iakovosvo/go-htmx-contacts-app/services"
import "fmt"

templ UpdateContactItemOob(contact services.Contact) {
	<div hx-swap-oob="outerHTML" id={ fmt.Sprintf("contact-%s", contact.ID) }>
		@ContactItem(contact)
	</div>
}
